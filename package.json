{
  "name": "whatsapp-crm",
  "version": "1.0.0",
  "description": "Multi-user WhatsApp CRM",
  "main": "server/index.js",
  "scripts": {
    "dev": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
